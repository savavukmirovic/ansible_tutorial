---

- hosts: all
  become: true
  tasks:

  - name: update repository index
    dnf:
      update_cache: true

  - name: install httpd package
    dnf: 
      name: httpd
      state: present 

  - name: start and enable httpd
    service:
      name: httpd
      state: started
      enabled: true
